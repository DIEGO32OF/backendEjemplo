# Proyecto de configuracion de un backend 
para verificacion de codigo es un simple CRUD 

# Proyecto construido con Node.JS
 para ejecutarlo es necesario correr `npm install` y posteriormente `npm start`
